const Loader = () => (
    <div className="absolute top-0 left-0 w-screen h-screen bg-black bg-opacity-50 flex items-center justify-center">
        <div className="w-8 h-8 bg-white animate-spin"></div>
    </div>
);

export default Loader;
